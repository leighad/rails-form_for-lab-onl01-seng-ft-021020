<h2>Student Index Page:</h2>

<ul>
    <% @students.each do |student| %>
        <li><%= link_to student.last_name.capitalize, student_path(student) %>, <%= link_to student.first_name.capitalize, student_path(student) %></li>
    <% end %>
</ul>

<%= params.inspect %>